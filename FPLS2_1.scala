def main(args: Array[String])={
    var k, i, j = 2;
    var m, n = 5;
    var f = 12.0f;
    var g = 4.0f;
    var c = 'X'
    println(k+12*m);
    println(m/j);
    println(n%j);
    println(m/j*j);
    println(f+10*5+g);
    i = i+1; //++i = i+1 
    println(i*n);
}


/* COMPARE THE JAVA AND SCALA PROGRAMMING LANGUAGES

    In Scala, the '++i' (pre-increment) , 'i++' (post-increment) ,
    '--i' (pre-decrement) and 'i--' (post-decrement) operators are 
    not available as they are in langiages like C++ or Java. 
    Instead, we must use the '+=' operator to increment aa variable.

*/